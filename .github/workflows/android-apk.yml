name: Build StrawIO VoiceChat Android APK

on:
  workflow_dispatch:
    inputs:
      api_base_url:
        description: "Backend API URL baked into the APK"
        required: false
        default: "http://10.0.2.2:3000"
  push:
    branches: ["main", "fix/flutter-android-v2"]
    paths:
      - "mobile/**"
      - ".github/workflows/android-apk.yml"

jobs:
  build-apk:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          cache: true

      - name: Prepare Android project
        working-directory: mobile
        run: |
          flutter create --platforms=android --org com.strawio --project-name emp_control_app .
          flutter pub get

      - name: Flutter analyze
        working-directory: mobile
        run: flutter analyze --no-fatal-infos

      - name: Flutter test
        working-directory: mobile
        run: flutter test

      - name: Build release APK
        working-directory: mobile
        env:
          API_BASE_URL: ${{ github.event.inputs.api_base_url || 'http://10.0.2.2:3000' }}
        run: flutter build apk --release --dart-define=API_BASE_URL="$API_BASE_URL"

      - name: Upload release APK
        uses: actions/upload-artifact@v4
        with:
          name: StrawIO-VoiceChat-release-apk
          path: mobile/build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error
